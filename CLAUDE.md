# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 目的

AIエージェント開発の作業スペース。**2026-09-08時点で、動いている案件は2つある。**

| # | 案件 | 状態 |
|---|---|---|
| **①** | **財務予測AIマルチエージェント** | 実装ほぼ完了・検証中。**継続決定済み** |
| **②** | **インターン面談準備の仕組み** | 設計段階。2026-09-08着手 |

**① 財務予測AI**（神永単独推進）
助成・受託事業の予算がいくら残り、年度末にどこへ着地するかを予測する。**視点の異なる3体のAIに別々の材料を渡して独立に予測させ、上位のAIが統合する**——これが本プロジェクトの中核方針であり、単なる計算処理に落とさないことをユーザーが明確に決めている。
**2026-09-07に、AIの役割は「予測」ではなく「診断・仮説検証」を主軸とする方針で合意した。**

**② インターン面談準備**
石川さんがインターン42人（稼働は約半数）と個人面談をする際の準備を肩代わりする。
9/7の打ち合わせで浮上した「業務が回る仕組みづくり」の最初の一手。詳細は下部の専用セクション。

---

## 🚨 最優先の安全ルール — データの取り扱い

**`data/` 配下のファイルを Read / cat / pdftotext で内容表示してはいけない。**

会社の実データ（決算・仕訳帳・見積書）が入っている。氏名・日単価・取引先名を含み、**あなたが読んだ内容はすべて外部APIへ送信される**。門番（Python）を通す前に読めば、守るべきものを守る前に社外へ出すことになる。

**②の面談記録も同じ扱い。** 人事評価に関わる会話は財務データより機微な可能性がある。
音声の文字起こしは **whisper.cpp のローカル実行**で行い、外部APIへ音声を送らないこと
（`geminiメモ/2026_09_07_石川さん打ち合わせ_文字起こし.md` がその手順で作られている）。

### 門番の設計原則（2026-08-19確定）

- **ホワイトリスト方式を使う。** 「危ないものを列挙して塞ぐ」方式は原理的に漏れる——列挙し忘れたものが必ず出る。安全側の設計は「安全と確認できたものだけを通す」。当初はブロックリスト方式だったが、この理由で反転させた
- **データの経路を数え漏らさない。** 経路を1つ見落とすと防御全体が無効になる。氏名は見積書だけでなく**仕訳帳の摘要**にも入る
- **緩く始めて後で締めるのは不可逆。** 厳しい側から始めること

### 触ってよいもの / いけないもの

| パス | 可否 | 中身 |
|---|---|---|
| `data/` | ❌ **読むな** | 会社の生データ（仕訳帳CSV 3件・見積書PDF 5件） |
| `geminiメモ/*.m4a` | ❌ **読むな** | 打ち合わせ音声。文字起こしはローカルで |
| `agent/masked/` | ⭕ 読んでよい | 門番通過後。ただし対応表を含むのでgit管理外 |
| `agent/output/` | ⭕ 読んでよい | 中間・最終出力。git管理外 |
| `agent/scenarios/` | ⭕ 読んでよい | 検証用の合成データ。ただし `truth.json` は**エージェントに渡さない** |
| `agent/data/sample_*` `template_*` | ⭕ 自由 | ダミーと、石川さんへ渡すテンプレート |
| `geminiメモ/` | ⭕ 読んでよい | 打ち合わせメモ。gitignore対象なので公開しない |

### マスキングの手順

```bash
python3 agent/masking.py             # 仕訳帳CSV → agent/masked/<PID>_journal.csv
python3 agent/masking.py --review    # 伏字にした語の候補を出す（語彙を育てる用・公開厳禁）
python3 agent/pdf_extract.py         # 見積書PDF → agent/masked/<PID>_budget_raw.txt
python3 agent/verify_mask.py         # 検査（本文を読まずに集計値だけ返す）
```

門番の許可語は `agent/allow_words_extra.txt`（339語・git追跡下）で育てる。
**氏名・団体名・地名は絶対に追加しない** — 許可語は組み合わさるため、
「起業」と「支援」を許可すると、それを名乗る団体名も素通りする。

**`verify_mask.py` の①②③がすべて0でなければ社外に出さない。** 検査自体が漏洩経路にならないよう、この道具は本文を表示しない設計になっている。同じ理由で、マスク結果の確認を「中身を見て判断する」形でやってはいけない。

---

# ① 財務予測AIマルチエージェント

## パイプラインの構成

実行手順の詳細は `.claude/skills/fc-forecast/SKILL.md`（調整AIの手順書）にある。**予測を依頼されたらまずこれを読むこと。**

```
生データ → ①門番(Python) → ②収集AG → ③証拠分割(Python)
        → ④3視点AG(並列・独立) → ⑤集計(Python) → ⑥調整AI
```

### 設計上の絶対ルール

1. **3視点AGを互いに議論させない。** 討論型は単純なCoTにすら負ける（ICLR 2025）。独立に判断させ、統合は調整AIだけが行う
2. **3視点に同じ証拠を配らない。** 同一証拠だと同調（herding）に堕して単体と変わらなくなる。`split_evidence.py` で物理的に分割する。**「見ないでください」と頼む形にしない**——ファイルにあれば読めてしまう
3. **数値はPython、解釈はLLM。** 確定計算・検算・分割の制御はPythonの担当。逆に、割れた理由の解釈をPythonにさせない
4. **門番はPython。** LLMマスキングは原理的に破綻する

### エージェント（`.claude/agents/*.md`）

| 定義 | 役割 |
|---|---|
| `fc-data-collector` | 予算と実績を読み、対応づけ、矛盾を `uncertain` に記録 |
| `fc-actuals` | このペースが続いたら？（**実績のみ**／予算は渡さない） |
| `fc-plan` | 計画どおり実行されたら？（**予算のみ**／月次推移は渡さない） |
| `fc-constraint` | そもそも執行可能なのは？（**費目の性質・期限**） |
| `fc-critical` / `fc-neutral` / `fc-advocate` | **旧版（性格軸）。置き換え済みだが削除許可は未取得** |

旧3体を消す場合は**必ずユーザーに確認**すること（削除は明示的許可が必須）。

---

## 現在地（2026-09-08時点）

### 動いている

- 実データで **6ステップが完走**（P3・財団案件・2025年度）
- 門番：仕訳帳CSV・見積書PDFとも対応。検査①②③が全ファイル0
- PDF自動取り込みは手作業の書き起こしと**主要6項目が完全一致**
- 予測の台帳（`ledger.py`）— 金額入りは社内、比率のみgit追跡

### P3の最新結果（2026-09-02実行）

予算 4,237,552円／実績 423,866円／経過率25%に対し消化率10%。

| 視点 | 着地見込み | 予算比 |
|---|---|---|
| 実績AG | 1,695,464円 | 40.0% |
| 制約AG | 3,480,642円 | 82.1% |
| 計画AG | 4,237,552円 | 100.0% |

乖離度 **0.730**（閾値0.3）→ 収束せず。統合値は中央値の 3,480,642円。

### 🚨 最大の発見 — 予測ではなくデータの欠損

**3案件すべてで、予算の主要費目が仕訳帳に1行も存在しない。**

| 案件 | 予算 | 実績 | 状況 |
|---|---|---|---|
| P1 | 未確定 | 15,687円 | 広告宣伝費のみ |
| P2 | 2,398,000円 | 48,318円 | **予算7費目すべてに対し実績0円** |
| P3 | 4,237,552円 | 423,866円 | 人件費・支援金が0円（予算の73%） |

→ **経理への確認事項**：人件費と支援金は未執行なのか、この仕訳帳に載らない
経路で処理されているのか。**この1点で解釈が正反対になる。**
P3固有ではなく経理の運用に起因する可能性が高い。

### ✅ 検証（2026-09-04）— 設計の中核に数字がついた

`scenarios.py` で**正解が構成上わかるデータ**を作って測った。1年ぶんの取引を生成し
3ヶ月目で切るので、真の着地は生成時点で確定している。

| シナリオ | 仕訳帳の被覆率 | 乖離度 | 誤差 | 季節ナイーブ |
|---|---|---|---|---|
| 01_順調 | 100% | **0.110**（収束） | **3.8%** | 9.1% |
| 05_仕訳漏れ | 33% | **0.875**（要確認） | **17.0%** | 69.7% |

**重要なのは誤差ではなく、乖離度が8倍違うこと。** 3体の食い違いはどの局面でも
大きいのではなく、**データが実態を覆えていないときだけ広がった**＝外れているときに
「信用するな」と言えている。

**🚫 このシナリオ結果に合わせて閾値やプロンプトを調整してはいけない。**
それをやると「作者の想像によく当たるAI」ができ、測定値だけが上がる。
直してよいのは壊れている箇所と明らかな論理の誤りだけ。

```bash
python3 agent/scenarios.py        # 6シナリオを生成
python3 agent/score_scenarios.py  # 採点（誤差・判定・較正・各AGの得意分野）
```

```bash
python3 agent/split_evidence.py 01_順調   # シナリオ名を渡すとシナリオ側を分割する
```

### 🚨 04_予算超過で構造的欠陥が判明（2026-09-08・**未修正**）

3件目にして**初めて単純外挿に負けた**。真の着地は予算比112%だが、3体とも下振れした。

| シナリオ | 被覆率 | 乖離度 | 中央値の誤差 | ナイーブ | 勝敗 |
|---|---|---|---|---|---|
| 01_順調 | 100% | 0.110（収束） | 3.8% | 9.1% | ✅ |
| 05_仕訳漏れ | 33% | 0.875（要確認） | 17.0% | 69.7% | ✅ 圧勝 |
| **04_予算超過** | 100% | **0.105（収束）** | **10.7%** | **9.1%** | ❌ **敗北** |

**原因：2体が原理的に予算超過を予測できない。**

- **計画AG** — 定義に「予算額を超える見積もりは出さないこと」と書いてある。
  実際は1,484,100円と計算したうえで1,452,000円へ切り下げていた
- **制約AG** — 「執行可能な**上限**」を積み上げる役割上、控除しかしない。構造的に超えられない
- **実績AG** — 外挿なので超えられる。101.8%と出した（唯一まともに動いた）

**さらに危険なのは、乖離度が0.105と低かったこと。** 「収束＝信用してよい」と表示されるが、
その一致は**共通の天井**によるものだった。

> **意見が揃うことは、正しさの証拠ではない。全員が同じ盲点を持つときも、意見は揃う。**

05で得た「乖離度は較正されている」の反例。乖離度が機能するのは**データ欠損のとき**で、
**全員が同じ制約を共有している場合は機能しない**。

### 提案した修正（**ユーザー判断待ち・未実施**）

1. **計画AGの上限を外す** — 論理の誤り。問いは「計画どおりならいくらか」で、超えるなら超えると言うべき
2. **調整AIに規則を追加** — いずれかのAGが `over_budget` を報告したら、中央値が予算以下でも「予算内」と結論しない
3. **乖離度の解釈を書き換え** — 低い乖離度は「共通の盲点がないか確認したうえで」信用する

**⚠️ 上の「シナリオに合わせて調整するな」という規則との線引きが論点。**
1は明確に論理の誤りだが、2と3は解釈の余地がある。**線引きをAIが一人で動かすと規則が無意味になる**ため、
ユーザーの判断を仰いでいる状態。

### 分かった適用範囲

- **データが実態を覆っていないとき**（05）→ 3視点が圧勝
- **単にペースが速いだけのとき**（04）→ 単純外挿に負ける。割り算で足りる

**この仕組みが効くのは「データが信用できるか怪しいとき」。** 弱点というより、いつ使うべきかが分かった。

未実行シナリオ：02_立ち上がり遅延 / **03_構造的な使い残し** / 06_期末集中。
**次は03** — 制約AGの主戦場なので、04で見つかった天井の影響を切り分けられる。

### ❌ できていないこと

**実データでの答え合わせは一度もしていない。** 上で測れたのは「機械が正しく
推論できるか」であって、「**この組織で当たるか**」は別物。混ぜないこと。
**年度末の確定実績があれば即座に採点できる**（手持ちデータは2025年度＝終了済み）。

```bash
python3 agent/ledger.py settle P3 2025-06 <確定額>
```

比較基準は季節ナイーブ（実績累計÷経過率）。これに勝てないなら3体を並べる意味がない。

その他：スキャンPDF・Excelは読めない／P1の予算PDFが未特定（**推測で紐づけない**）／
自動実行不可（人間が起動）／旧エージェント3体が残存（削除許可待ち）。

→ 残作業は末尾の**「未決の宿題」**にまとめてある。

---

## 2026-09-07 石川さんとの打ち合わせ — 役割の拡張

音源と全文は `geminiメモ/2026_09_07_石川さん打ち合わせ_文字起こし.md`（gitignore対象）。

### 何が変わったか

財務予測AIは**継続**（「それ自体は続けようね」と明言）。変わったのは**その外側**で、
会社が機能別に分かれ、インターン制度が階層化される（プレインターン→インターン→
アソシエイト→社員級）のに合わせ、**業務そのものを回すAIの仕組みづくり**が
神永の次の半年の主題になった。

- **タスク出し自体をAIにやらせる。** 会社の仕事を知っているAIがミーティングの議論を
  ナレッジ化し、そこから**チケットを切る**。プレインターンはそれを消化する
  （「チケット10枚でインターン昇格を検討」という定量運用の構想）
- **週次レポートを人ではなくAIが出す。** 本人の「今週これやりました」を廃し、
  進捗が勝手に出ていて誰でも見られる状態にする。それを見ながら議論する場を録音し、
  また戻す。石川さんが口頭で伝えた理由は「文字に書くと監視しているように読めるから」

### 依頼されたもの（次回打ち合わせまで）

**これまでの3ヶ月の知見を、雑でよいので全量書き出す。** 要求仕様が通常と逆なので注意。

- **人間用に整えない。短くしない。長くてよい**
- **AIが読む文書として書く**（「AIに見せる文章の方がAIは出しやすいはず」）
- **時系列と経緯を残す**（何月何日に何をしたまで）
- 使えるか・どう繋がるかは判断しなくてよい。**かけた時間を切り離すのがもったいない**、が理由

`docs/2026-09_3ヶ月レポート.md` は土台になるが、**あれは人間向けに整えた排出用**で
今回の要求とは別物。粒度を上げた版が要る。

### 保留になっている論点

- **Claude か GPT か。** 石川さんは単価面でGPTに傾いている（10月からの価格が読めない）。
  ただし8/16にClaude Codeを選んだ理由（mdで定義でき、独立コンテキストが保証される）は
  単価とは別軸なので、判断材料として提示すること
- **プロジェクト横断の可否。** Claudeは横断させない、GPTは少し超える。機能別会社化との
  相性を石川さんが見たがっている
- **完全自動化には行かない。** OpenClaw等は「エンジニアではない」「全情報を見せたくない」
  ため不採用。**ローカル／スタンドアローンも不要**。Claude か GPT の中で
  エージェント的機能を3つ立ち上げる方針は変わらず
- レポート第7節の6つの依頼（経理確認・再エクスポート等）は**今回まったく触れられていない。
  未回答のまま**

---

# ② インターン面談準備の仕組み（2026-09-08 着手・設計段階）

## 解こうとしている問題

石川さんはインターン42人（稼働は約半数）と個人面談をするが、**1人ずつと話す前に
その人のことを思い出す時間が取れない**。全員の活動を把握したいわけではない。

**ボトルネックは情報収集ではなく、面談の準備。** だから作るのは
「活動の可視化ダッシュボード」ではなく**面談10分前に開く1人分の1枚**。
的が小さく、データが多少欠けていても機能する。

## 絶対に外してはいけない前提

**監視にしてはいけない。** 石川さんは依頼をわざわざ音声で送ってきた。理由は本人の言葉で
**「文字で書くと監視しているように読めてしまうから」**。
この設計を「**行動ログを集める**」という立て方にした時点で失敗する。

- 石川さんは「誰でも見られるオープンな状態に置く」と発言している
- 本人が報告を書く形は**明確に否定されている**（負担だから）

## 前提条件（ユーザー確認済み・2026-09-08）

| 項目 | 状態 | 効いてくること |
|---|---|---|
| ツール | **Slack → Google へ移行中** | **Slack連携を作ってはいけない。**捨てる資産になる |
| チケット制 | **構想だけ。まだ何も無い** | 状態遷移を拾う案は現時点で使えない |
| 最初の的 | **石川さんの面談準備** | 決定済み |

## 提案した設計（**未着手**）

**入力は「面談そのもの」にする。新しい入力を作らない。**

```
面談を録音 → ローカルで文字起こし（whisper.cpp） → 個人ごとのファイルに要約を追記
                                                        ↓
                                    次の面談の10分前に、その人の1枚を生成
```

利点：インターンの負担ゼロ／**拾うのはその人自身の面談だけなので監視にならない**／
道具の移行に影響されない／チケット制の完成を待たずに今日から動く。

初回は何も出ない。**2回目から効く**——「前回何を話し、何をやると言ったか」。
42人分でこれを覚えているのは無理で、石川さんが実際に困っているのはここ。

### 出力する1枚の形（案）

```
◯◯さん   前回: 8/12（27日前）
── 前回やると言ったこと
   ・□□の資料を今月中に      → その後の言及なし
   ・△△さんに連絡            → 8/20の全体MTGで完了と発言
── 前回つまずいていたこと
   ・◇◇の進め方が分からない、と話していた
── 今回聞くとよさそうなこと
   1. □□の資料、止まっている理由は何か
   2. ◇◇はその後どうなったか
   3. 次に何をやりたいか
※ AIが生成。事実誤認があれば直してください
```

**「やったことの一覧」ではなく「話すべきことの候補」にする。**
同じデータから作れるが、前者は評価の材料に見え、後者は面談の準備に見える。

### 未決の3点（**ユーザー・石川さんの判断待ち**）

1. **本人にも1枚を見せるか** — **これが監視かどうかの分岐。**
   見せれば「共通の議題」、見せなければ「調書」。**AIは間違うので本人が直せる形が要る**
2. **録音は誰が回すか** — **録音することを本人に伝えるのは必須。**
   黙って録るのは仕組みの目的と正面から矛盾する
3. **文字起こしをローカルのままにするか** — 人事の会話は財務より機微かもしれない。
   財務側で作った門番の考え方がそのまま効く

### 試作の材料は既にある

`geminiメモ/2026_09_07_石川さん打ち合わせ_文字起こし.md` が whisper.cpp のローカル実行で
作られている。**これを材料に神永さん自身の1枚を試作すれば、形の妥当性が15分で分かる。**
自分のデータなので誰の許可も要らず、石川さんに見せるときも現物が1枚あるほうが早い。

---

# 共通（両案件にかかる話）

## 引き継ぎの仕組み

**2026-08-26決定：アカウント借用は行わず、ローカル環境での開発とマスキング導入を基本方針とする。** 3週間の開発で「1ヶ月借りる必要はない」と結論したため、8/5に申請した社内アカウント借用は不要になった。**あわせて、ClaudeによるMDファイル更新を引き継ぎの標準とすることが会社の決定事項になった** — 以後の更新は義務。

引き継ぎが成立するのは、**設計がすべてこのリポジトリのファイル側にあり、Claude側に状態を持たせていない**ため。

- `.claude/agents/*.md` と `.claude/skills/*/SKILL.md` が体制の実体。git追跡下にあり、cloneすれば復元される
- エージェントが状態を持たない設計は**過去時点の再現＝バックテスト**にも効いている
- 予測記録は `ledger.py` が2層に分ける。**金額入りの `agent/output/forecast_log.jsonl` は社内のみ、比率だけの `agent/forecast_index.md` はgit追跡下**。精度の履歴は設計とともに引き継がれ、金額は引き継がれない
- `~/.claude/CLAUDE.md`（個人のグローバル設定）はリポジトリ外なので他環境では効かない

### 排出（ナレッジの外出し）

`docs/` に置く。**人が読む用がHTML、機械が読む用がMarkdown**という分担。
`docs/2026-09_3ヶ月レポート.md` は対話履歴とgit履歴から生成したもので、手作りしていない。
石川さん側のエージェントへ投入する前提のため、**取引先名と個人名は除き、案件は
`P1`〜`P3` の記号にしてある**（金額は経緯が伝わらなくなるため残してある）。

**精度向上の実体は「過去の予測を読ませること」ではない。** それはアンカリングを起こし、予測が更新ではなく複製になる。必要なのは**予測と実績の対**から得た教訓を**mdファイルに書き戻す**こと。読み込みではなく書き込みが賢さの実体。

---

## 作業の進め方（ユーザーの意向）

- **ドキュメントより実装を優先する。** index.htmlの更新より、コードを少しずつ進めることを望んでいる
- **`git push` は頼まれるまでしない。** コミットも同様
- **AIを使うことにこだわる。** 「足し算・引き算だから手作業でよい」という指摘に対し、AI活用を維持する決定を下している（意思決定ボード論点6）
- **視点の異なるAIによる予測が目的。** 単なる計算をさせたいわけではない

## 権限ルール

- Web検索・リサーチは許可不要
- 以下は実行前に必ず許可を求める
  - HTTPなど暗号化されていないプロトコルへのアクセス
  - 課金が発生する可能性のある操作・APIの呼び出し
  - **ファイルの削除**（対象一覧を提示してから実行）

## 未決の宿題（2026-09-08時点・ここを見れば続きが分かる）

| # | 内容 | 誰が解けるか | 状態 |
|---|---|---|---|
| 1 | **知見の全量書き出し**（AI向け・長くてよい・時系列） | 自分 | **未着手・次回MTGまで** |
| 2 | 04の構造的欠陥の修正①②③ | **ユーザー判断待ち** | 提案済み |
| 3 | ②の未決3点（本人に見せるか／録音／ローカル） | ユーザー・石川さん | 提案済み |
| 4 | ②の試作（9/7の記録で1枚作ってみる） | 自分 | 未着手 |
| 5 | 03_構造的な使い残しシナリオ | 自分 | 未着手 |
| 6 | 財務エージェントを「診断主軸」に作り替える | 自分 | 未着手 |
| 7 | 経理への確認（人件費・支援金の計上経路） | 石川さん経由 | **3回依頼して未回答** |
| 8 | 年度末実績の再エクスポート | 石川さん経由 | 未回答 |
| 9 | 予算CSVテンプレートを石川さんへ渡す | 自分 | 作成済み・**未送付** |

**7と8が埋まらない限り、「この組織で当たるか」は永久に測れない。**
9は1分で送れて相手の作業を解放するので、優先度が高いわりに止まっている。

## リファレンスドキュメント

- `geminiメモ/` — 打ち合わせメモと文字起こし。**新しい指示はここに入ることが多いので、作業前に最新分を必ず確認する**（gitignore対象）
- `docs/2026-09_3ヶ月レポート.md` — 3ヶ月の記録。石川さんへ送付済み
- `claude_code_commands.md` — Claude Code のスラッシュコマンド・ショートカット一覧（v2.1.182時点）
- `index.html` — 研究ノート。「神永ライン」タブが現在の作業（概要／設計／実演／運用／ログ／残作業）
- `.claude/skills/fc-forecast/SKILL.md` — 調整AIの手順書。**予測作業の前に必読**
